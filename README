使用方法:

1.执行npm install -g typescript webpack(若全局环境中已经存在可省略该步骤)

2.在根目录中执行yarn install

3.npm link typescript

4.执行yarn run webpack-product(目前使用webpack-dev会出现react压缩报错的问题,建议使用webpack-product)

5.打开dist文件夹中的index.html即可查看效果